{
  "name": "deep-pulse-functions",
  "description": "Firebase Cloud Functions for DEEP Pulse — Web3 Brand Engagement Platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint --max-warnings=0 .",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "bs58": "^6.0.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-google": "^0.14.0"
  }
}
